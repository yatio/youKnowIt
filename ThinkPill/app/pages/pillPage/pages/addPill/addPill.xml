   <Page xmlns="http://schemas.nativescript.org/tns.xsd"  navigatingTo="onNavigatingTo">
    <StackLayout>
        <!-- <TextField id="id" hint="Numeric ID" text="{{ id }}" />
        <TextField id="title" hint="title" text="{{ title }}" />
        <TextView id="body" hint="body" text="{{ body }}" />
        <TextView id="ticker" hint="ticker" text="{{ ticker }}" />
        <StackLayout orientation="horizontal">
            <Button text="Schedule" tap="{{ schedule }}" />
        </StackLayout> -->


        <TextField hint="Titulo" text="{{ titulo }}"  />
        
        <!-- <TextField id="dia" hint="Dia" text="{{ dia }}" keyboardType="number" secure="true" maxLength="2" /> -->
        <!-- <TextField id="dia" hint="Dia" text="{{ dia }}" keyboardType="number" maxLength="2" /> -->
        <!-- <TextField id="lastname" hint="Mes" text="{{ mes }}" keyboardType="number" maxLength="2" /> -->
        <!--droop down  -->

            <DatePicker day="{{ day }}" month="{{ month }}" year="{{ year }}" maxDate="2031" minDate="2018,1,1" verticalAlignment="center"/>
            <!-- <DatePicker day="1" month="10" year="2017" verticalAlignment="center"/> -->
            <TimePicker id="timePicker" hour="{{ hora }}" minute="{{ minutos }}"/>

        <!-- <TextField id="year" hint="Año" text="{{ year }}" keyboardType="number" maxLength="4" />  -->
        
        <!-- <TextField id="hora" hint="Hora" text="{{ hora }}" /> -->
        <!-- <TextField id="minutos" hint="Minutos" text="{{ minutos }}" /> -->
        <!-- <StackLayout orientation="horizontal"> -->
            <Button text="Insert" tap="{{ insert }}" />
             <!-- <Button text="Select" tap="{{ select }}" />       -->
            <!--El select existe solo para debuggin  -->
        <!-- </StackLayout>  -->
    </StackLayout> 
</Page>  
<!-- no letras en los q son int, arreglar paq solo pongan numeros -->