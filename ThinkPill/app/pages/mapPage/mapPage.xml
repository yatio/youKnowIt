 <Page xmlns="http://www.nativescript.org/tns.xsd" xmlns:maps="nativescript-google-maps-sdk" navigatingTo="onNavigatingTo">

    <maps:mapView 
			latitude="{{ latitude }}" 
			longitude="{{ longitude }}" 
			mapAnimationsEnabled="{{ mapAnimationsEnabled }}"
			zoom= '{{ zoom }}' 
			bearing="{{ bearing }}" 
			tilt="{{ tilt }}" 
			padding="{{ padding }}" 
			mapReady="onMapReady"  
			markerSelect="onMarkerSelect" 
			markerBeginDragging="onMarkerBeginDragging"
			markerEndDragging="onMarkerEndDragging" 
			markerDrag="onMarkerDrag"
			cameraChanged="onCameraChanged" />
</Page>